What is left to do:

Mandatory:
Fix Segfault / invalid read size
Norminette cleanup

Optional:
2-3 were added to parsing, you can add the goal post the function to call the ending function already program
Collision